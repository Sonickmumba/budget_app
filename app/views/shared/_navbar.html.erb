<% if user_signed_in? %>
    <nav class="navbar  register">
      <div class="container-fluid">
        <div class="fluid-container login w-100">
          <div class="row w-100">
            <h2 class="py-3 text-center text-white">Categories</h2>
          </div>
        </div>
        <div class="d-flex position-absolute left-0">
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-link p-1 btn-sm text-white text-decoration-none" %>
        </div>
      </div>
    </nav>
<% end %>